<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.day_planner">


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".task_display" />
        <activity android:name=".note_display" />
        <activity android:name=".LockScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Task2" />

        <activity android:name=".Main" />
        <activity android:name=".mainactivity2" />
        <activity
            android:name=".mainactivity"
            android:parentActivityName=".LockScreen">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.day_planner.LockScreen" />
        </activity>
    </application>

</manifest>